- name: "Uninstall docker tools and environment"
  hosts: 'all'
  gather_facts: false
  remote_user: startx
  become_method: sudo
  become: yes
  tasks:
  - name: Remove docker tools
    yum:
      name:
        - docker
      state: absent